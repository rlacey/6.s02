times = round(10:590/49:600);

theta0s = zeros(1, 50);
theta1s = zeros(1, 50);
cis = zeros(1, 50);

tvec = tend(1:10);
Ivec = Iend(1:10);

for i = 1:50 
    i
    length(tvec)
    theta0s(i), theta1s(i), cis(i) = glucosepredict( tvec, Ivec );
    tvec = times(1:i));
    Ivec = Iend(1:times(i));
end

plot(times, theta0s, times, theta0s);